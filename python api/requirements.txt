# Core FastAPI dependencies (compatible versions)
fastapi==0.100.1
uvicorn[standard]==0.23.2
pydantic==1.10.12  # Older version that doesn't require Rust
python-multipart==0.0.6

# HTTP and async networking
aiohttp==3.8.5
httpx==0.24.1

# Scheduling and time handling
schedule==1.2.1

# Data validation and serialization
pydantic-settings==0.2.3

# Logging and utilities
loguru==0.7.2

# Security and cryptography (for future use)
cryptography==41.0.3
bcrypt==4.0.1

# Optional: OpenAI integration (uncomment if using OpenAI actions)
# openai==0.28.1

# Optional: Google integrations (uncomment if using Gmail triggers)
# google-api-python-client==2.86.0
# google-auth-oauthlib==1.0.0

# Optional: Telegram integration (uncomment if using Telegram triggers)
# python-telegram-bot==20.3

# Optional: Slack integration (uncomment if using Slack triggers)
# slack-sdk==3.23.0

# Optional: Database integrations (uncomment based on your database choice)
# asyncpg==0.28.0  # PostgreSQL
# aiomysql==0.1.1  # MySQL
# motor==3.1.2     # MongoDB

# Optional: Redis for caching and sessions (uncomment if using Redis)
# redis==4.5.5
# aioredis==2.0.1

# Development and testing dependencies
pytest==7.4.0
pytest-asyncio==0.21.1
httpx==0.24.1  # For testing FastAPI endpoints

# Code quality and linting
black==23.7.0
isort==5.12.0
flake8==6.0.0
mypy==1.5.1


